 import java.util.*;
import java.text.*;
class d extends loa
{public static String Date()
{ Date now=new Date();
 DateFormat obx=DateFormat.getDateTimeInstance(DateFormat.FULL,DateFormat.FULL);
 return(obx.format(now));
}
}