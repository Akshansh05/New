package PROJECT.afr;
 import java.util.*;
import java.text.*;
class d 
{public String Date()
{ Date now=new Date();
 DateFormat obx=DateFormat.getDateTimeInstance(DateFormat.FULL,
 DateFormat.FULL);
 return(obx.format(now));
}
}